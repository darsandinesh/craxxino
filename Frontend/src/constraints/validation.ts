import { Account } from "../interface/IAccount";

const validation = {
    accountValidation: async (data: Account) => {
        try {
            console.log(data);
        } catch (error) {

        }
    }
}

export default validation;